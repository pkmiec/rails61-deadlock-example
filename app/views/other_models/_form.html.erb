<%= form_with(model: other_model) do |form| %>
  <% if other_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(other_model.errors.count, "error") %> prohibited this other_model from being saved:</h2>

      <ul>
        <% other_model.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
